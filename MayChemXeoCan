local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Workspace = game:GetService("Workspace")
local LP = Players.LocalPlayer

local oldUI = pcall(gethui) and gethui():FindFirstChild("OmniGoldenSea2") or CoreGui:FindFirstChild("OmniGoldenSea2")
if oldUI then oldUI:Destroy() end

getgenv().AutoEggSea2 = false
getgenv().IsTweening = false
local currentTween = nil

local Sea2EggSpawns = {
    Vector3.new(-425, 73, 1836),
    Vector3.new(-479, 332, 532),
    Vector3.new(-2380, 72, -3130),
    Vector3.new(-5400, 48, -750),
    Vector3.new(1030, 140, -4850),
    Vector3.new(-5350, 15, -5100),
    Vector3.new(-6050, 15, -4900),
    Vector3.new(5900, 25, -6300),
    Vector3.new(-3000, 15, -10000),
    Vector3.new(3700, 15, -1000),
    Vector3.new(4700, 15, 2900),
    Vector3.new(-2200, 70, -2500)
}

local UI = Instance.new("ScreenGui")
UI.Name = "OmniGoldenSea2"
UI.ResetOnSpawn = false
UI.Parent = pcall(gethui) and gethui() or CoreGui

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 150, 0, 40)
Main.Position = UDim2.new(0.5, -75, 0, 20)
Main.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.Parent = UI

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 4)
local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = Color3.fromRGB(255, 200, 0)
Stroke.Thickness = 1.5

local Btn = Instance.new("TextButton")
Btn.Size = UDim2.new(1, -10, 1, -10)
Btn.Position = UDim2.new(0, 5, 0, 5)
Btn.BackgroundTransparency = 1
Btn.Text = "SEA 2 GOLDEN: OFF"
Btn.TextColor3 = Color3.fromRGB(255, 80, 80)
Btn.Font = Enum.Font.GothamBlack
Btn.TextSize = 11
Btn.Parent = Main

local function SnipeGoldenOnly()
    for _, v in pairs(Workspace:GetDescendants()) do
        local name = string.lower(v.Name)
        if string.find(name, "golden") and string.find(name, "egg") then
            local targetPart = v:IsA("Model") and (v:FindFirstChild("HumanoidRootPart") or v:FindFirstChild("Part") or v.PrimaryPart) or (v:IsA("BasePart") and v or nil)
            local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if hrp and targetPart then
                local dist = (hrp.Position - targetPart.Position).Magnitude
                local tInfo = TweenInfo.new(dist / 350, Enum.EasingStyle.Linear)
                local tween = TweenService:Create(hrp, tInfo, {CFrame = targetPart.CFrame})
                tween:Play()
                tween.Completed:Wait()
                task.wait(0.5)
            end
        end
    end
end

local function BypassTween(targetPos)
    local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local dist = (hrp.Position - targetPos).Magnitude
    if dist < 50 then return end
    getgenv().IsTweening = true
    local tweenInfo = TweenInfo.new(dist / 320, Enum.EasingStyle.Linear)
    currentTween = TweenService:Create(hrp, tweenInfo, {CFrame = CFrame.new(targetPos)})
    currentTween:Play()
    currentTween.Completed:Wait()
    getgenv().IsTweening = false
end

RunService.Stepped:Connect(function()
    if getgenv().IsTweening and LP.Character then
        for _, v in pairs(LP.Character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
        local hrp = LP.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
        end
    end
end)

Btn.MouseButton1Click:Connect(function()
    getgenv().AutoEggSea2 = not getgenv().AutoEggSea2
    if getgenv().AutoEggSea2 then
        Btn.Text = "SEA 2 GOLDEN: ON"
        Btn.TextColor3 = Color3.fromRGB(255, 215, 0)
    else
        Btn.Text = "SEA 2 GOLDEN: OFF"
        Btn.TextColor3 = Color3.fromRGB(255, 80, 80)
        if currentTween then currentTween:Cancel() end
        getgenv().IsTweening = false
    end
end)

task.spawn(function()
    while true do
        if getgenv().AutoEggSea2 then
            for _, pos in ipairs(Sea2EggSpawns) do
                if not getgenv().AutoEggSea2 then break end
                if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                    BypassTween(pos + Vector3.new(0, 60, 0))
                    SnipeGoldenOnly()
                    task.wait(2)
                end
            end
        end
        task.wait(1)
    end
end)
