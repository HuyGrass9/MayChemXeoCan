local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Workspace = game:GetService("Workspace")
local LP = Players.LocalPlayer

local oldUI = pcall(gethui) and gethui():FindFirstChild("OmniGoldenV9") or CoreGui:FindFirstChild("OmniGoldenV9")
if oldUI then oldUI:Destroy() end

getgenv().AutoEggUltra = false
getgenv().IsTweening = false
local currentTween = nil

local TweenPoints = {
    Vector3.new(3897.67, 13.59, -3543.17),
    Vector3.new(-5698.84, 126.27, -734.21),
    Vector3.new(-3186.04, 298.91, -10584.57),
    Vector3.new(855.27, 121.82, 1209.73),
    Vector3.new(6721.07, 294.63, -6970.51),
    Vector3.new(-5635.95, 42.40, -5197.75)
}

local ShipTarget = Vector3.new(920.27, 125.30, 32844.54)
local SafeReturn = Vector3.new(2293.10, 27.13, 910.90)

local UI = Instance.new("ScreenGui")
UI.Name = "OmniGoldenV9"
UI.ResetOnSpawn = false
UI.Parent = pcall(gethui) and gethui() or CoreGui

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 150, 0, 35)
Main.Position = UDim2.new(0.5, -75, 0, 15)
Main.BackgroundColor3 = Color3.fromRGB(5, 5, 10)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.Parent = UI

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 4)
local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = Color3.fromRGB(255, 215, 0)
Stroke.Thickness = 1.8

local Btn = Instance.new("TextButton")
Btn.Size = UDim2.new(1, 0, 1, 0)
Btn.BackgroundTransparency = 1
Btn.Text = "ABSOLUTE GOLDEN: OFF"
Btn.TextColor3 = Color3.fromRGB(255, 80, 80)
Btn.Font = Enum.Font.GothamBlack
Btn.TextSize = 10
Btn.Parent = Main

local function AbsoluteGoldenSnipe()
    for _, v in pairs(Workspace:GetDescendants()) do
        if v:IsA("Model") or v:IsA("BasePart") then
            local name = string.lower(v.Name)
            if string.find(name, "golden") and string.find(name, "egg") then
                local targetPart = v:IsA("Model") and (v:FindFirstChild("HumanoidRootPart") or v:FindFirstChild("Part") or v.PrimaryPart) or v
                local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                if hrp and targetPart then
                    local dist = (hrp.Position - targetPart.Position).Magnitude
                    local tInfo = TweenInfo.new(dist / 600, Enum.EasingStyle.Linear)
                    local tween = TweenService:Create(hrp, tInfo, {CFrame = targetPart.CFrame})
                    tween:Play()
                    tween.Completed:Wait()
                    task.wait(0.1)
                end
            end
        end
    end
end

local function InstantTeleport(targetPos)
    local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        if currentTween then currentTween:Cancel() end
        getgenv().IsTweening = false
        hrp.CFrame = CFrame.new(targetPos)
        task.wait(0.3)
    end
end

local function HitAndRunShip()
    InstantTeleport(ShipTarget)
    local timer = 0
    while timer < 5 and getgenv().AutoEggUltra do
        AbsoluteGoldenSnipe()
        task.wait(1)
        timer = timer + 1
    end
    if getgenv().AutoEggUltra then
        InstantTeleport(SafeReturn)
        task.wait(1)
    end
end

local function BypassTween(targetPos)
    local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local dist = (hrp.Position - targetPos).Magnitude
    if dist < 30 then return end
    getgenv().IsTweening = true
    local tInfo = TweenInfo.new(dist / 330, Enum.EasingStyle.Linear)
    currentTween = TweenService:Create(hrp, tInfo, {CFrame = CFrame.new(targetPos)})
    currentTween:Play()
    currentTween.Completed:Wait()
    getgenv().IsTweening = false
end

local function GetClosestPoint(currentPos, pointList)
    local closest = nil
    local minDist = math.huge
    local bestIndex = -1
    for i, pos in ipairs(pointList) do
        local dist = (currentPos - pos).Magnitude
        if dist < minDist then
            minDist = dist
            closest = pos
            bestIndex = i
        end
    end
    return closest, bestIndex
end

RunService.Stepped:Connect(function()
    if getgenv().IsTweening and LP.Character then
        for _, v in pairs(LP.Character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
        local hrp = LP.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
        end
    end
end)

Btn.MouseButton1Click:Connect(function()
    getgenv().AutoEggUltra = not getgenv().AutoEggUltra
    if getgenv().AutoEggUltra then
        Btn.Text = "ABSOLUTE GOLDEN: ON"
        Btn.TextColor3 = Color3.fromRGB(255, 215, 0)
    else
        Btn.Text = "ABSOLUTE GOLDEN: OFF"
        Btn.TextColor3 = Color3.fromRGB(255, 80, 80)
        if currentTween then currentTween:Cancel() end
        getgenv().IsTweening = false
    end
end)

task.spawn(function()
    while true do
        if getgenv().AutoEggUltra and LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
            local remainingPoints = {}
            for _, p in ipairs(TweenPoints) do
                table.insert(remainingPoints, p)
            end
            
            while #remainingPoints > 0 and getgenv().AutoEggUltra do
                local hrp = LP.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then break end
                
                local targetPos, index = GetClosestPoint(hrp.Position, remainingPoints)
                if targetPos then
                    table.remove(remainingPoints, index)
                    BypassTween(targetPos + Vector3.new(0, 40, 0))
                    
                    local t = 0
                    while t < 5 and getgenv().AutoEggUltra do
                        AbsoluteGoldenSnipe()
                        task.wait(1)
                        t = t + 1
                    end
                end
            end
            
            if getgenv().AutoEggUltra then
                HitAndRunShip()
            end
        end
        task.wait(1)
    end
end)
