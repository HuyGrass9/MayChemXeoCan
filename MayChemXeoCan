local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VIM = game:GetService("VirtualInputManager")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local LP = Players.LocalPlayer

pcall(function() if setfpscap then setfpscap(120) end end)

local ConfigName = "Macro_ZeroDelay_V1.json"
local Config = {
    ComboC = "1Z3XZ", 
    ComboC2 = "",
    HitboxSize = 15,
    Hitbox = false,
    BtnSize = 60,
    SetupMode = false,
    ShowC = false,
    ShowC2 = false,
    ESP = false,
    ViewTracer = false
}

pcall(function()
    if isfile and isfile(ConfigName) then
        local saved = HttpService:JSONDecode(readfile(ConfigName))
        for k, v in pairs(saved) do Config[k] = v end
    end
end)

local function SaveConfig()
    pcall(function() writefile(ConfigName, HttpService:JSONEncode(Config)) end)
end

getgenv().MacroHidden = false

local UIParent = nil
pcall(function() UIParent = gethui() end)
if not UIParent then pcall(function() UIParent = CoreGui end) end
if not UIParent then UIParent = LP:WaitForChild("PlayerGui") end

local oldUI = UIParent:FindFirstChild("Macro")
if oldUI then oldUI:Destroy() end

local UI = Instance.new("ScreenGui")
UI.Name = "Macro"
UI.ResetOnSpawn = false
UI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
UI.DisplayOrder = 9999
UI.Parent = UIParent

local ToggleIcon = Instance.new("ImageButton")
ToggleIcon.Size = UDim2.new(0, 45, 0, 45)
ToggleIcon.Position = UDim2.new(0.5, -22, 0, 10)
ToggleIcon.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
ToggleIcon.Image = "rbxassetid://10709769508"
ToggleIcon.BorderSizePixel = 0
ToggleIcon.Active = true
ToggleIcon.Draggable = true
Instance.new("UICorner", ToggleIcon).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", ToggleIcon).Color = Color3.fromRGB(40, 40, 45)
ToggleIcon.Parent = UI

local function CreateActionBtn(name, yPos, color, showKey)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, Config.BtnSize, 0, Config.BtnSize)
    btn.Position = UDim2.new(0.8, 0, yPos, 0)
    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    btn.BackgroundTransparency = 0.2
    btn.Text = name
    btn.TextColor3 = color
    btn.Font = Enum.Font.GothamBlack
    btn.TextSize = math.clamp(Config.BtnSize * 0.4, 15, 45)
    btn.Visible = Config[showKey]
    btn.Active = true
    btn.Selectable = false
    btn.Draggable = Config.SetupMode
    Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)
    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = color
    stroke.Thickness = 2
    btn.Parent = UI
    return btn, stroke
end

local ExecBtnC, StrokeC = CreateActionBtn("C", 0.4, Color3.fromRGB(255, 60, 60), "ShowC")
local ExecBtnC2, StrokeC2 = CreateActionBtn("C2", 0.6, Color3.fromRGB(60, 180, 255), "ShowC2")

local Menu = Instance.new("Frame")
Menu.Size = UDim2.new(0, 280, 0, 360)
Menu.Position = UDim2.new(0.1, 0, 0.5, -180)
Menu.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
Menu.BorderSizePixel = 0
Menu.Active = true
Menu.Visible = false
Menu.ClipsDescendants = true
Instance.new("UICorner", Menu).CornerRadius = UDim.new(0, 6)
local MenuStroke = Instance.new("UIStroke", Menu)
MenuStroke.Color = Color3.fromRGB(40, 40, 45)
MenuStroke.Thickness = 1
Menu.Parent = UI

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "MACRO"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Menu

local UILine = Instance.new("Frame")
UILine.Size = UDim2.new(1, 0, 0, 1)
UILine.Position = UDim2.new(0, 0, 0, 40)
UILine.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
UILine.BorderSizePixel = 0
UILine.Parent = Menu

local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, 0, 0, 35)
TabBar.Position = UDim2.new(0, 0, 0, 41)
TabBar.BackgroundTransparency = 1
TabBar.Parent = Menu

local TabFrames = {}
local TabBtns = {}
local Indicator = Instance.new("Frame")
Indicator.Size = UDim2.new(0.33, 0, 0, 2)
Indicator.Position = UDim2.new(0, 0, 1, -2)
Indicator.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
Indicator.BorderSizePixel = 0
Indicator.Parent = TabBar

local function CreateTab(name, xPos, isActive)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.33, 0, 1, 0)
    btn.Position = UDim2.new(xPos, 0, 0, 0)
    btn.BackgroundTransparency = 1
    btn.Text = name
    btn.TextColor3 = isActive and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(150, 150, 150)
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 11
    btn.Parent = TabBar
    
    local frame = Instance.new("ScrollingFrame")
    frame.Size = UDim2.new(1, 0, 1, -76)
    frame.Position = UDim2.new(0, 0, 0, 76)
    frame.BackgroundTransparency = 1
    frame.Visible = isActive
    frame.ScrollBarThickness = 2
    frame.Parent = Menu
    
    TabBtns[name] = btn
    TabFrames[name] = frame
    
    btn.MouseButton1Click:Connect(function()
        for k, v in pairs(TabFrames) do v.Visible = (k == name) end
        for k, v in pairs(TabBtns) do v.TextColor3 = (k == name) and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(150, 150, 150) end
        TweenService:Create(Indicator, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Position = UDim2.new(xPos, 0, 1, -2)}):Play()
    end)
    return frame
end

local TabCombat = CreateTab("COMBAT", 0, true)
local TabVisual = CreateTab("VISUALS", 0.33, false)
local TabSet = CreateTab("SETTINGS", 0.66, false)

local function CreateTextInput(parent, yPos, text, configKey)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.9, 0, 0, 20)
    lbl.Position = UDim2.new(0.05, 0, 0, yPos)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = parent
    
    local box = Instance.new("TextBox")
    box.Size = UDim2.new(0.9, 0, 0, 35)
    box.Position = UDim2.new(0.05, 0, 0, yPos + 25)
    box.BackgroundColor3 = Color3.fromRGB(18, 18, 20)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.Font = Enum.Font.GothamBold
    box.TextSize = 14
    box.Text = Config[configKey]
    box.ClearTextOnFocus = false
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)
    Instance.new("UIStroke", box).Color = Color3.fromRGB(40, 40, 45)
    box.Parent = parent
    
    box.FocusLost:Connect(function() Config[configKey] = box.Text; SaveConfig() end)
    return box
end

CreateTextInput(TabCombat, 5, "Sequence C:", "ComboC")
CreateTextInput(TabCombat, 75, "Sequence C2:", "ComboC2")

getgenv().MCXC_ActiveSlider = nil

local function CreateSlider(parent, yPos, text, min, max, configKey, isInt)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.9, 0, 0, 20)
    lbl.Position = UDim2.new(0.05, 0, 0, yPos)
    lbl.BackgroundTransparency = 1
    lbl.Text = text .. ": " .. tostring(Config[configKey])
    lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = parent

    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(0.9, 0, 0, 6)
    bg.Position = UDim2.new(0.05, 0, 0, yPos + 25)
    bg.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    Instance.new("UICorner", bg).CornerRadius = UDim.new(1, 0)
    bg.Parent = parent

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((Config[configKey] - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    fill.Parent = bg

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text = ""
    btn.Parent = bg

    local dragging = false
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            if not getgenv().MCXC_ActiveSlider then getgenv().MCXC_ActiveSlider = configKey; dragging = true end
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            if dragging and getgenv().MCXC_ActiveSlider == configKey then dragging = false; getgenv().MCXC_ActiveSlider = nil; SaveConfig() end
        end
    end)
    RunService.RenderStepped:Connect(function()
        if dragging and getgenv().MCXC_ActiveSlider == configKey then
            local mousePos = UserInputService:GetMouseLocation().X
            local relPos = mousePos - bg.AbsolutePosition.X
            local pct = math.clamp(relPos / bg.AbsoluteSize.X, 0, 1)
            local val = min + (max - min) * pct
            if isInt then val = math.floor(val) else val = math.floor(val * 100) / 100 end
            fill.Size = UDim2.new(pct, 0, 1, 0)
            lbl.Text = text .. ": " .. tostring(val)
            Config[configKey] = val
            
            if configKey == "BtnSize" then
                ExecBtnC.Size = UDim2.new(0, val, 0, val)
                ExecBtnC.TextSize = math.clamp(val * 0.4, 15, 45)
                ExecBtnC2.Size = UDim2.new(0, val, 0, val)
                ExecBtnC2.TextSize = math.clamp(val * 0.4, 15, 45)
            end
        end
    end)
end

CreateSlider(TabVisual, 5, "Hitbox Size", 5, 30, "HitboxSize", true)
CreateSlider(TabSet, 5, "UI Button Scale", 40, 120, "BtnSize", true)

local function CreateToggle(parent, x, y, text, configKey, cb)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.42, 0, 0, 35)
    btn.Position = UDim2.new(x, 0, 0, y)
    btn.BackgroundColor3 = Config[configKey] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(20, 20, 22)
    btn.Text = text
    btn.TextColor3 = Config[configKey] and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    Instance.new("UIStroke", btn).Color = Color3.fromRGB(40, 40, 45)
    btn.Parent = parent
    
    btn.MouseButton1Click:Connect(function()
        Config[configKey] = not Config[configKey]
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Config[configKey] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(20, 20, 22)}):Play()
        TweenService:Create(btn, TweenInfo.new(0.2), {TextColor3 = Config[configKey] and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(200, 200, 200)}):Play()
        SaveConfig()
        if cb then cb(Config[configKey]) end
    end)
end

CreateToggle(TabCombat, 0.05, 145, "SHOW C", "ShowC", function(v) if not getgenv().MacroHidden then ExecBtnC.Visible = v end end)
CreateToggle(TabCombat, 0.53, 145, "SHOW C2", "ShowC2", function(v) if not getgenv().MacroHidden then ExecBtnC2.Visible = v end end)
CreateToggle(TabVisual, 0.05, 55, "HITBOX", "Hitbox")
CreateToggle(TabVisual, 0.53, 55, "ESP RADAR", "ESP")
CreateToggle(TabVisual, 0.05, 100, "VIEW TRACER", "ViewTracer")
CreateToggle(TabSet, 0.05, 55, "SETUP UI", "SetupMode", function(v)
    ExecBtnC.Draggable = v; ExecBtnC2.Draggable = v
end)

local HideBtn = Instance.new("TextButton")
HideBtn.Size = UDim2.new(0.42, 0, 0, 35)
HideBtn.Position = UDim2.new(0.53, 0, 0, 55)
HideBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
HideBtn.Text = "HIDE ALL"
HideBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
HideBtn.Font = Enum.Font.GothamBold
HideBtn.TextSize = 11
Instance.new("UICorner", HideBtn).CornerRadius = UDim.new(0, 6)
HideBtn.Parent = TabSet

local InstTxt = Instance.new("TextLabel")
InstTxt.Size = UDim2.new(0.9, 0, 0, 80)
InstTxt.Position = UDim2.new(0.05, 0, 0, 105)
InstTxt.BackgroundTransparency = 1
InstTxt.Text = "HƯỚNG DẪN:\nNhập combo (VD: 3XZ1C).\nSố = Vũ khí, Chữ = Skill.\nAI Bật show C để bật nút Macro hỗ trợ combo \nAI Tinh chỉnh vị trí nút Macro cho dễ tiếp cận ( 4 ngón )"
InstTxt.TextColor3 = Color3.fromRGB(180, 180, 180)
InstTxt.TextWrapped = true
InstTxt.Font = Enum.Font.GothamMedium
InstTxt.TextSize = 11
InstTxt.Parent = TabSet

HideBtn.MouseButton1Click:Connect(function()
    getgenv().MacroHidden = true
    UI.Enabled = false
end)

ToggleIcon.MouseButton1Click:Connect(function() Menu.Visible = not Menu.Visible end)

local function GetToolType(tool)
    if not tool then return "0" end
    local n = string.lower(tool.Name)
    local tt = tool:FindFirstChild("ToolTip")
    local v = tt and tostring(tt.Value) or ""
    local vl = string.lower(v)
    
    if string.find(vl, "melee") or string.find(v, "Cận") or string.find(v, "cận") or string.find(v, "Võ") or string.find(v, "võ") or string.find(n, "human") or string.find(n, "art") or string.find(n, "claw") or string.find(n, "step") or (string.find(n, "dragon") and string.find(n, "talon")) then return "1" end
    if string.find(vl, "fruit") or string.find(v, "Trái") or string.find(v, "trái") or string.find(v, "Quỷ") or string.find(v, "quỷ") or (string.find(n, "dragon") and not string.find(n, "talon") and not string.find(n, "trident")) or string.find(n, "kitsune") or string.find(n, "dough") or string.find(n, "leopard") or string.find(n, "buddha") or string.find(n, "portal") or string.find(n, "rumble") then return "2" end
    if string.find(vl, "sword") or string.find(v, "Kiếm") or string.find(v, "kiếm") or string.find(v, "Đao") or string.find(v, "đao") or string.find(n, "katana") or string.find(n, "blade") or string.find(n, "sword") or string.find(n, "tushita") or string.find(n, "yama") or string.find(n, "saber") or string.find(n, "anchor") or string.find(n, "dark") or string.find(n, "cdk") or string.find(n, "trident") then return "3" end
    if string.find(vl, "gun") or string.find(v, "Súng") or string.find(v, "súng") or string.find(v, "Pháo") or string.find(v, "pháo") or string.find(n, "rifle") or string.find(n, "gun") or string.find(n, "soul") or string.find(n, "kabucha") or string.find(n, "serpent") or string.find(n, "guitar") then return "4" end
    return "0"
end

local function IsSkillBusy(char)
    if not char then return false end
    for _, v in ipairs(char:GetChildren()) do
        if v:IsA("BoolValue") and v.Value == true then
            local n = string.lower(v.Name)
            if string.find(n, "busy") or string.find(n, "usingskill") or string.find(n, "charging") or string.find(n, "holding") or string.find(n, "power") then
                return true
            end
        end
    end
    return false
end

local function ClearEnemyStun(char)
    if not char then return end
    local stuns = {"stun", "cant", "nodash", "nojump"} 
    for _, v in ipairs(char:GetChildren()) do
        if v:IsA("BoolValue") and v.Value == true then
            local n = string.lower(v.Name)
            for _, tag in ipairs(stuns) do
                if string.find(n, tag) then v.Value = false end
            end
        end
    end
end

local lockC, lockC2 = false, false

local function ExecuteAIPacing(comboStr, stroke, baseColor, isC2)
    stroke.Color = Color3.fromRGB(255, 255, 255)
    task.spawn(function()
        task.wait(0.05)
        local s = string.upper(comboStr)
        
        local firstChar = string.sub(s, 1, 1)
        if firstChar == "1" or firstChar == "2" or firstChar == "3" or firstChar == "4" then
            local curTool = LP.Character:FindFirstChildOfClass("Tool")
            if curTool and GetToolType(curTool) == firstChar then
                s = string.sub(s, 2)
            end
        end
        
        for i = 1, #s do
            local char = string.sub(s, i, i)
            if char == "1" or char == "2" or char == "3" or char == "4" then
                local curTool = LP.Character:FindFirstChildOfClass("Tool")
                if not curTool or GetToolType(curTool) ~= char then
                    local kc
                    if char == "1" then kc = Enum.KeyCode.One
                    elseif char == "2" then kc = Enum.KeyCode.Two
                    elseif char == "3" then kc = Enum.KeyCode.Three
                    elseif char == "4" then kc = Enum.KeyCode.Four
                    end
                    
                    if kc then
                        VIM:SendKeyEvent(true, kc, false, game)
                        task.wait(0.01)
                        VIM:SendKeyEvent(false, kc, false, game)
                        
                        local w = tick()
                        while tick() - w < 0.5 do
                            local t = LP.Character:FindFirstChildOfClass("Tool")
                            if t and GetToolType(t) == char then break end
                            RunService.Heartbeat:Wait()
                        end
                        task.wait(0.02)
                    end
                end
            elseif char == "Z" or char == "X" or char == "C" or char == "V" or char == "F" then
                ClearEnemyStun(LP.Character)
                local kc = Enum.KeyCode[char]
                
                VIM:SendKeyEvent(true, kc, false, game)
                task.wait(0.01)
                VIM:SendKeyEvent(false, kc, false, game)
                
                local waitReg = tick()
                local triggered = false
                while tick() - waitReg < 0.4 do
                    if IsSkillBusy(LP.Character) then triggered = true; break end
                    RunService.Heartbeat:Wait()
                end
                
                if triggered then
                    local waitAnim = tick()
                    while IsSkillBusy(LP.Character) and tick() - waitAnim < 2.5 do
                        RunService.Heartbeat:Wait()
                    end
                    task.wait(0.05)
                else
                    break
                end
                ClearEnemyStun(LP.Character)
            end
        end
        stroke.Color = baseColor
        if not isC2 then lockC = false else lockC2 = false end
    end)
end

ExecBtnC.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        if Config.SetupMode or lockC then return end
        lockC = true
        ExecuteAIPacing(Config.ComboC, StrokeC, Color3.fromRGB(255, 60, 60), false)
    end
end)

ExecBtnC2.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        if Config.SetupMode or lockC2 then return end
        lockC2 = true
        ExecuteAIPacing(Config.ComboC2, StrokeC2, Color3.fromRGB(60, 180, 255), true)
    end
end)

local function SafeGetPlayerInfo(plr)
    local lvl, race, hp, mhp = "?", "?", 0, 0
    pcall(function()
        if plr:FindFirstChild("Data") then
            if plr.Data:FindFirstChild("Level") then lvl = tostring(plr.Data.Level.Value) end
            if plr.Data:FindFirstChild("Race") then race = tostring(plr.Data.Race.Value) end
        end
        if plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            if hum then hp = math.floor(hum.Health); mhp = math.floor(hum.MaxHealth) end
        end
    end)
    return lvl, race, hp, mhp
end

RunService.Heartbeat:Connect(function()
    pcall(function()
        if LP.Character then
            ClearEnemyStun(LP.Character)
            
            if getgenv().MacroHidden then
                local cT = LP.Character:FindFirstChildOfClass("Tool")
                if cT and string.find(string.lower(cT.Name), "katana") then
                    getgenv().MacroHidden = false
                    UI.Enabled = true
                end
            end
            
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= LP and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    local root = plr.Character.HumanoidRootPart
                    local dist = (root.Position - LP.Character.HumanoidRootPart.Position).Magnitude
                    
                    if Config.Hitbox and not getgenv().MacroHidden then
                        local hs = tonumber(Config.HitboxSize) or 15
                        root.Size = Vector3.new(hs, hs, hs)
                        root.Transparency = 0.8
                        root.CanCollide = false
                    end
                    
                    local espId = "ESP_" .. plr.Name
                    local espGui = root:FindFirstChild(espId)
                    
                    if Config.ESP and dist <= 1000 and not getgenv().MacroHidden then
                        if not espGui then
                            espGui = Instance.new("BillboardGui")
                            espGui.Name = espId
                            espGui.Size = UDim2.new(0, 250, 0, 75)
                            espGui.StudsOffset = Vector3.new(0, 3, 0)
                            espGui.AlwaysOnTop = true
                            local txt = Instance.new("TextLabel")
                            txt.Size = UDim2.new(1, 0, 1, 0)
                            txt.BackgroundTransparency = 1
                            txt.Font = Enum.Font.GothamBold
                            txt.TextSize = 11
                            txt.TextStrokeTransparency = 0
                            txt.Parent = espGui
                            espGui.Parent = root
                        end
                        
                        local lvl, race, hp, mhp = SafeGetPlayerInfo(plr)
                        local weap = "None"
                        local pTool = plr.Character:FindFirstChildOfClass("Tool")
                        if pTool then weap = pTool.Name end
                        
                        local txtNode = espGui:FindFirstChildOfClass("TextLabel")
                        if txtNode then
                            txtNode.Text = string.format("[Lv.%s] %s\nHP: %d/%d\nRace: %s | W: %s\nDist: %dm", lvl, plr.Name, hp, mhp, race, string.sub(weap, 1, 15), math.floor(dist))
                            txtNode.TextColor3 = (mhp > 0 and hp / mhp < 0.4) and Color3.fromRGB(255, 50, 50) or Color3.fromRGB(255, 255, 255)
                        end
                    else
                        if espGui then espGui:Destroy() end
                    end
                    
                    local head = plr.Character:FindFirstChild("Head") or root
                    local lineId = "TracerLine_" .. plr.Name
                    local arrId = "TracerArrow_" .. plr.Name
                    local line = head:FindFirstChild(lineId)
                    local arr = head:FindFirstChild(arrId)
                    
                    if Config.ViewTracer and dist <= 1000 and not getgenv().MacroHidden then
                        if not line then
                            line = Instance.new("CylinderHandleAdornment")
                            line.Name = lineId
                            line.Adornee = head
                            line.Color3 = Color3.fromRGB(255, 50, 50)
                            line.Radius = 0.6
                            line.Height = 50
                            line.ZIndex = 5
                            line.Transparency = 0.2
                            line.AlwaysOnTop = true
                            line.CFrame = CFrame.new(0, 0, -25) 
                            line.Parent = head
                        end
                        if not arr then
                            arr = Instance.new("ConeHandleAdornment")
                            arr.Name = arrId
                            arr.Adornee = head
                            arr.Color3 = Color3.fromRGB(255, 50, 50)
                            arr.Radius = 1.8
                            arr.Height = 3.5
                            arr.ZIndex = 5
                            arr.Transparency = 0.2
                            arr.AlwaysOnTop = true
                            arr.CFrame = CFrame.new(0, 0, -51.5) 
                            arr.Parent = head
                        end
                    else
                        if line then line:Destroy() end
                        if arr then arr:Destroy() end
                    end
                end
            end
        end
    end)
end)

if LP.Character then LP.Character.ChildAdded:Connect(function() ClearEnemyStun(LP.Character) end) end
LP.CharacterAdded:Connect(function(char) char.ChildAdded:Connect(function() ClearEnemyStun(char) end) end)
